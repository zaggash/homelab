apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bazarr
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: "1.4.0"
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    podAnnotations:
      backup.velero.io/backup-volumes-excludes: media
    image:
      repository: ghcr.io/onedr0p/bazarr
      tag: 1.2.0@sha256:052551fecd3f851d3c73c5539df77a8d393133d8f1c83e760d8c009dd9b59f58
    env:
      TZ: "Europe/Paris"
    service:
      main:
        ports:
          http:
            port: 6767
            protocol: TCP
    ingress:
      main:
        enabled: false
    persistence:
      config:
        enabled: true
        retain: true
        existingClaim: "bazarr-config"
        mountPath: /config
      media:
        enabled: true
        retain: true
        existingClaim: "all-data"
        subPath: tvshows
        mountPath: /media/tvshows
