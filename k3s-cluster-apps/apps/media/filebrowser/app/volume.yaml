---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-config
spec:
  storageClassName: nfs-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100M
