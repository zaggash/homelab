---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: drone
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`ci.${SECRET_CLUSTER_DOMAIN}`)
      services:
        - kind: Service
          name: drone
          port: 8000
