apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: uptime-kuma
spec:
  chart:
    spec:
      interval: 5m
      chart: app-template
      version: "1.2.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  interval: 5m
  install:
    crds: Create
  maxHistory: 3
  upgrade:
    crds: CreateReplace
  values:
    image:
      repository: louislam/uptime-kuma
      tag: 1.19.4-alpine@sha256:ae75312ea48c0cf3bd94a1e40b2d4022c8b170a961dc361ce4d39d70ab4c946d
    env:
      TZ: "Europe/Paris"
      PUID: 1000
      PGID: 1000
    service:
      main:
        ports:
          http:
            type: ClusterIP
            port: 3001
    ingress:
      main:
        enabled: false
    persistence:
      data:
        enabled: true
        existingClaim: uptime-kuma-data
