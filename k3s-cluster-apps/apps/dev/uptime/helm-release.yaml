apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: uptime-kuma
spec:
  chart:
    spec:
      interval: 5m
      chart: app-template
      version: "1.2.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  interval: 5m
  install:
    crds: Create
  maxHistory: 3
  upgrade:
    crds: CreateReplace
  values:
    image:
      repository: louislam/uptime-kuma
      tag: 1.19.5-alpine@sha256:6de586e872c1a611cf2d035f48d3334547f71144e593d284c0b6f1f35797746c
    env:
      TZ: "Europe/Paris"
      PUID: 1000
      PGID: 1000
    service:
      main:
        ports:
          http:
            type: ClusterIP
            port: 3001
    ingress:
      main:
        enabled: false
    persistence:
      data:
        enabled: true
        existingClaim: uptime-kuma-data
