apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: error-pages
spec:
  interval: 15m
  chart:
    spec:
      chart:  app-template
      version: "1.4.0"
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/tarampampam/error-pages
      tag: 2.22@sha256:bc1696597411071d1085127105783216aa336ed4379f5165176d8e25380348dc
    env:
      TZ: "Europe/Paris"
      TEMPLATE_NAME: "ghost"
      SHOW_DETAILS: "true"
    service:
      main:
        ports:
          http:
            port: 8080
    ingress:
      main:
        enabled: false
