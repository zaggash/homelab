services:
  homebox:
    container_name: homebox
    image: ghcr.io/sysadminsmedia/homebox:0.23.0@sha256:eabc0de4dbbcfb59289397d88ad80f768abf2e08c03f8c197be35ba953a10826
    labels:
      folder.view2: Tools
      net.unraid.docker.icon: ${SECRET_APPDATA}/icons/homebox.png
      traefik.enable: true
      traefik.docker.network: homelab_tools
      traefik.http.routers.homebox.rule: Host(`homebox.${SECRET_DOMAIN}`)
      traefik.http.routers.homebox.entryPoints: websecure
      traefik.http.services.homebox.loadbalancer.server.port: 7745
    volumes:
      - "${SECRET_APPDATA}/homebox:/data/"
    user: ${UID}:${GID}
    environment:
      TZ: ${TZ}
      HBOX_LOG_LEVEL: info
      HBOX_LOG_FORMAT: text
      HBOX_WEB_MAX_FILE_UPLOAD: 10
      HBOX_OPTIONS_ALLOW_ANALYTICS: false
      HBOX_OPTIONS_ALLOW_REGISTRATION: false
    networks:
      - tools
    restart: always
