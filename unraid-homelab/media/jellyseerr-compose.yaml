services:
  jellyseer:
    container_name: jellyseerr
    image: fallenbagel/jellyseerr:2.2.2@sha256:fe72f4b341fa5c3275826a39aebfff23cbd552c956b0ab2d51dfba6a49b528f8
    labels:
      folder.view: Media
      net.unraid.docker.icon: ${SECRET_APPDATA}/icons/jellyseerr.png
      traefik.enable: true
      traefik.http.routers.jellyseerr.rule: Host(`req.${SECRET_DOMAIN}`)
      traefik.http.routers.jellyseerr.entryPoints: websecure
      traefik.http.services.jellyseerr.loadbalancer.server.port: 5055
    volumes:
      - "${SECRET_APPDATA}/jellyseerr:/app/config"
    user: ${UID}:${GID}
    environment:
      TZ: ${TZ}
      LOG_LEVEL: INFO
    networks:
      proxy:
      media:
    restart: always
