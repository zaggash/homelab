services:
  calibre:
    container_name: calibre
    image: ghcr.io/linuxserver/calibre:7.21.0@sha256:1dad366bde7d0d4d705ba9a4cfdf89343b960ac2c834e8b5f21d2090b9e4da38
    labels:
      folder.view: Media
      net.unraid.docker.icon: ${SECRET_APPDATA}/icons/calibre.png
      traefik.enable: true
      traefik.http.routers.calibre.rule: Host(`calibre.${SECRET_HOME_DOMAIN}`)
      traefik.http.routers.calibre.entryPoints: websecure
      traefik.http.services.calibre.loadbalancer.server.port: 8080
    volumes:
      - "${SECRET_APPDATA}/calibre:/config"
      - "${SECRET_DATADIR}/ebooks:/books"
    environment:
      TZ: ${TZ}
      PUID: ${UID}
      PGID: ${GID}
    networks:
      - proxy
    restart: always
