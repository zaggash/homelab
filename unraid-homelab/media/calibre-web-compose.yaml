services:
  calibre-web:
    container_name: calibre-web
    image: ghcr.io/linuxserver/calibre-web:2021.12.16@sha256:ef318f14a6581e0d35b40d1e4784c66a7ec9d628b1580c0a318be6f7d4db05f3
    labels:
      traefik.enable: true
      traefik.http.routers.calibre-web.rule: Host(`books.${SECRET_DOMAIN}`)
      traefik.http.routers.calibre-web.entryPoints: websecure
      traefik.http.services.calibre-web.loadbalancer.server.port: 8083
    volumes:
      - "${SECRET_APPDATA}/calibre-web:/config"
      - "${SECRET_APPDATA}/calibre-web/cache:/app/calibre-web/cps/cache"
      - "${SECRET_DATADIR}/ebooks:/books"
    environment:
      TZ: ${TZ}
      PUID: ${UID}
      PGID: ${GID}
      DOCKER_MODS: linuxserver/mods:universal-calibre
    networks:
      - proxy
    restart: always
