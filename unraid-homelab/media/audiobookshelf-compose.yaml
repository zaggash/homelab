services:
  audiobookshelf:
    container_name: audiobookshelf
    image: ghcr.io/advplyr/audiobookshelf:2.36.1@sha256:3528a93b6442ffe54bd46771bbbab7c97084e1101071586d9dc2254f30bb4358
    labels:
      folder.view3: Media
      net.unraid.docker.icon: file://${SECRET_APPDATA}/icons/audiobookshelf.png
      traefik.enable: true
      traefik.docker.network: homelab_media
      traefik.http.routers.audiobookshelf.rule: Host(`audiobooks.${SECRET_DOMAIN}`)
      traefik.http.routers.audiobookshelf.entryPoints: websecure
      traefik.http.services.audiobookshelf.loadbalancer.server.port: 80
    volumes:
      - "${SECRET_APPDATA}/audiobookshelf/config:/config"
      - "${SECRET_APPDATA}/audiobookshelf/metadata:/metadata"
      - "${SECRET_DATADIR}/media/audiobooks:/audiobooks"
    environment:
      TZ: ${TZ}
    networks:
      - media
    restart: always
